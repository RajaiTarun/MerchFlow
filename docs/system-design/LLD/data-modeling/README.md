# Data Modeling

One doc: [polyglot-persistence.md](polyglot-persistence.md) — the actual Postgres `orders` schema (including three denormalized columns, and the specific reasoning for each) and the Mongo `Item` schema, plus why the catalog and the transactional data are split across two different database engines at all.
